version: '3.8'

services:
  visitor-manager:
    build: .
    container_name: unifi-visitor-manager
    ports:
      - "8080:8080"
    volumes:
      - ./database:/app/data
    restart: unless-stopped